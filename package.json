{
  "name": "@ensdomains/dnssec-oracle",
  "version": "0.0.8",
  "requires": true,
  "lockfileVersion": 1,
  "devDependencies": {
    "@ensdomains/buffer": "^0.0.5",
    "@ensdomains/solsha1": "^0.0.1",
    "dnsprovejs": "0.0.9",
    "eth-gas-reporter": "^0.1.2",
    "rfc4648": "^1.1.0",
    "solium": "^1.0.4",
    "truffle": "^5.0.0"
  },
  "scripts": {
    "pretest": "truffle compile",
    "test": "truffle test",
    "watch": "npm-watch",
    "lint": "solium --dir ./contracts",
    "prepublishOnly": "truffle compile && truffle networks --clean"
  },
  "dependencies": {
    "dns-packet": "^5.0.4"
  }
}
