{
  "name": "dnssec-oracle",
  "version": "0.0.1",
  "requires": true,
  "lockfileVersion": 1,
  "devDependencies": {
    "eth-gas-reporter": "^0.1.2",
    "solium": "^1.0.4",
    "truffle": "^4.0.0"
  },
  "scripts": {
    "pretest": "truffle compile",
    "test": "truffle test",
    "watch": "npm-watch",
    "lint": "solium --dir ./contracts",
    "prepare": "truffle compile"
  },
  "dependencies": {
  }
}
